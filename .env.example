# SimPilot Environment Variables
# Copy this file to .env.local and fill in your values.
#
# IMPORTANT:
# - This is a Vite SPA. Any `VITE_*` variable is bundled into the browser build.
# - Treat all `VITE_*` values as PUBLIC (do not store secrets here).

# Environment: local | preview | production
VITE_APP_ENV=local

# Google OAuth Client ID
# Get this from Google Cloud Console: https://console.cloud.google.com/apis/credentials
VITE_GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com

# SimBridge (optional)
# VITE_SIMBRIDGE_URL=https://your-simbridge.example.com

# Microsoft 365 / SharePoint (optional - MS Graph integration via MSAL)
# If these are unset, MS integration is disabled.
# VITE_MSAL_CLIENT_ID=your-azure-client-id
# VITE_MSAL_TENANT_ID=your-azure-tenant-id
# VITE_MSAL_REDIRECT_URI=http://localhost:5173
# VITE_MSAL_SHAREPOINT_SITE_ID=your-sharepoint-site-id
# VITE_MSAL_SHAREPOINT_DRIVE_ID=your-sharepoint-drive-id
# VITE_MSAL_SHAREPOINT_ROOT_PATH=/Shared Documents/SimPilot

# Node-only tooling (optional)
# VITE_SIMPILOT_DATA_ROOT=C:\\path\\to\\workbooks
